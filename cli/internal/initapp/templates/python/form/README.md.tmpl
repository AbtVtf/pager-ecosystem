# {{.Name}}

A PagerOS form-starter app — `Form` widget, POST handler, confirmation screen.

## Run

```sh
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python app.py --host 127.0.0.1 --port 8000
```
