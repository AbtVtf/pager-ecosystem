# {{.Name}}

A push-reminder starter — skeleton form that *would* schedule a push notification. The actual `send_push` call is commented out because it needs marketplace-registered Ed25519 + X25519 keys; see `examples/push-reminder` for a runnable version.
